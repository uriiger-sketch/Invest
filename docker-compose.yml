services:
  invest:
    build: .
    container_name: invest
    env_file: .env
    ports:
      - "${STREAMLIT_PORT:-8501}:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: ["python", "-m", "invest.cli", "serve"]
